#include "main.h"

/**
 * _abs - Computes the absolute value of an int.
 * @n: n is an int.
 *
 * Return: Integer
 */

int _abs(int n)
{
	if (n > 0)
	{
	return (n);
	}
	else if (n < 0)
	{
	return (-n);
	}
	else
	{
	return (n);
	}
}
