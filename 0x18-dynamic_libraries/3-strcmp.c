#include "main.h"
#include <string.h>
/**
 * _strcmp - Function that compares two strings.
 * @s1: String1 to compare.
 * @s2: Strong2 to compare.
 * Return: 0
 */
int _strcmp(char *s1, char *s2)
{
	int result;

	result = strcmp(s1, s2);
	return (result);
}
